# pre-commit configuration for wb_humanoid_mpc
repos:
  - repo: local
    hooks:
      - id: clang-format-and-black
        name: Code Formatter (make format)
        entry: tools/hooks/pre-commit
        language: script
        types_or: [c++, c, python]
        pass_filenames: false
