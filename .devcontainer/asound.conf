# Null ALSA configuration to avoid "cannot find card '0'" errors inside headless containers.
# This file will be mounted as /etc/asound.conf inside the container by docker-compose.
pcm.!default {
  type null
}

ctl.!default {
  type hw
  card 0
}
