{
  "name": "Whole Body MPC Dev Container",
<<<<<<< HEAD
  "dockerComposeFile": [
    "docker-compose.yml"
=======
  "initializeCommand": [
    "bash",
    "-c",
    "xhost +local: && echo USER_ID=$(id -u) > \"${localWorkspaceFolder}/.env\" && echo GROUP_ID=$(id -g) >> \"${localWorkspaceFolder}/.env\""
  ],
  "build": {
    "dockerfile": "${localWorkspaceFolder}/docker/Dockerfile",
    "context": "..",
    "target": "base",
    "args": {
      "WB_HUMANOID_MPC_DIR": "/wb_humanoid_mpc_ws",
      "PYTHON_VERSION": "3.12",
      "USER_ID": "${localEnv:USER_ID}",
      "GROUP_ID": "${localEnv:GROUP_ID}",
      "GIT_USER_NAME": "${command:git config --global user.name}",
      "GIT_USER_EMAIL": "${command:git config --global user.email}"
    }
  },
  "runArgs": [
    "--rm",
    "-it",
    "--net=host",
    "--privileged",
    "-u",
    "${localEnv:USER_ID}:${localEnv:GROUP_ID}",
    "-e",
    "DISPLAY",
    "-e",
    "QT_X11_NO_MITSHM=1",
    "-e",
    "XDG_RUNTIME_DIR=${localEnv:XDG_RUNTIME_DIR}",
    "-e",
    "GIT_USER_NAME=$(git config --global user.name)",
    "-e",
    "GIT_USER_EMAIL=$(git config --global user.email)",
    "-v",
    "/tmp/.X11-unix:/tmp/.X11-unix",
    "-v",
    "${localEnv:XDG_RUNTIME_DIR}:${localEnv:XDG_RUNTIME_DIR}"
>>>>>>> 9c77bd3 (saving progress)
  ],
  "service": "app",
  "remoteUser": "ubuntu",
  "containerUser": "ubuntu",
  "updateRemoteUserUID": false,
  "workspaceFolder": "/wb_humanoid_mpc_ws/src/wb_humanoid_mpc",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "/bin/bash",
            "icon": "terminal-bash"
          }
        },
        "editor.formatOnSave": true,
        "files.trimTrailingWhitespace": true,
        "[cpp]": {
          "editor.defaultFormatter": "xaver.clang-format"
        },
        "[c]": {
          "editor.defaultFormatter": "xaver.clang-format"
        }
      },
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "ms-vscode-remote.remote-containers",
        "ms-azuretools.vscode-docker",
        "ms-python.python",
        "ms-python.vscode-pylance",
        "xaver.clang-format",
        "eamodio.gitlens",
        "GitHub.copilot",
        "GitHub.copilot-chat",
        "twxs.cmake",
        "cheshirekow.cmake-format",
        "ms-toolsai.jupyter-keymap",
        "ms-vscode-remote.remote-ssh",
        "ms-vscode-remote.remote-ssh-edit",
        "ms-vscode.remote-explorer",
        "sankethdev.vscode-proto",
        "Continue.continue",
        "ms-vscode.cpptools-extension-pack",
        "ms-vscode.makefile-tools",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ]
    }
  },
  "mounts": [
    "source=${localWorkspaceFolder}/../..,target=/wb_humanoid_mpc_ws,type=bind,consistency=delegated",
    "source=${localEnv:HOME}/.gitconfig,target=/tmp/host.gitconfig,type=bind,consistency=cached,readOnly=true",
    "source=${localEnv:HOME}/.ssh,target=/tmp/host_ssh,type=bind,consistency=cached"
  ],
<<<<<<< HEAD
  "containerEnv": {
    "DISPLAY": ":99",
    "ROS_DOMAIN_ID": "0",
    "LIBGL_ALWAYS_SOFTWARE": "1",
    "MESA_LOADER_DRIVER_OVERRIDE": "llvmpipe",
    "LIBGL_ALWAYS_INDIRECT": "0"
  },
  "forwardPorts": [
    5901,
    6080
  ],
  "portsAttributes": {
    "6080": {
      "label": "noVNC (RViz)",
      "onAutoForward": "notify"
    },
    "5901": {
      "label": "VNC",
      "onAutoForward": "silent"
    }
  },
  "postCreateCommand": "bash -c 'chmod +x ${containerWorkspaceFolder}/.devcontainer/post_create.sh && ${containerWorkspaceFolder}/.devcontainer/post_create.sh'"
}
=======
  "updateRemoteUserUID": true,
  "postCreateCommand": "git config --global user.name \"$GIT_USER_NAME\" && git config --global user.email \"$GIT_USER_EMAIL\" && git config --global --add safe.directory /wb_humanoid_mpc_ws/src/wb_humanoid_mpc && curl -o .git-completion.bash https://raw.githubusercontent.com/git/git/master/contrib/completion/git-completion.bash && echo 'source /opt/ros/jazzy/setup.bash' >> ~/.bashrc && echo 'source ./.git-completion.bash' >> ~/.bashrc"
}
>>>>>>> 9c77bd3 (saving progress)
